@AGENTS.md

# TODO
- Add scrolling news/announcements section to the homepage (about.md) with milestones like PhD start, paper submissions, TA appointments. The announcements config is already in about.md frontmatter (currently `enabled: false`), and news items go in `_news/`.
- Dependabot alerts (6 vulnerabilities) are from upstream al-folio's Gemfile.lock — blocked by jekyll-terser git dependency. Wait for upstream fix, then merge and resolve.
- Re-enable projects page once first-author publications are out.
- Set up Google Scholar profile and add to socials.yml once more publications are available.
