<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://aritrac.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://aritrac.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-22T17:28:43+00:00</updated><id>https://aritrac.com/feed.xml</id><title type="html">blank</title><subtitle>Portfolio page for Aritra Chakrabarty, a PhD student in CS at CU Boulder researching safe AI for robotics </subtitle><entry><title type="html">Deep Transformer Q Networks — A paper analysis</title><link href="https://aritrac.com/blog/2024/deep-transformer-q-networksa-paper-analysis/" rel="alternate" type="text/html" title="Deep Transformer Q Networks — A paper analysis"/><published>2024-10-31T18:04:38+00:00</published><updated>2024-10-31T18:04:38+00:00</updated><id>https://aritrac.com/blog/2024/deep-transformer-q-networksa-paper-analysis</id><content type="html" xml:base="https://aritrac.com/blog/2024/deep-transformer-q-networksa-paper-analysis/"><![CDATA[<h3>Paper review: Deep Transformer Q Networks</h3> <h4>..and all you need to know about Transformers and Reinforcement Learning to understand it</h4> <p>The goal of this article is to analyze the paper “Deep Transformer Q-Networks for Partially Observable Reinforcement Learning” by Kevin Esslinger, Robert Platt, and Christopher Amato while learning some of the background and history along the way.</p> <p><a href="https://arxiv.org/abs/2206.01078">Deep Transformer Q-Networks for Partially Observable Reinforcement Learning</a></p> <h3>Background</h3> <p>Let’s first start with a little bit of information regarding reinforcement learning.</p> <p>What is RL? It’s a type of machine learning where an agent learns to take the best sequence of actions in an environment to maximize its final reward. This scenario is well captured by the image below.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/375/1*9hNJMhihj3gHJUT-1JwV_w.png"/><figcaption>Reinforcement Learning Scenario | Source: <a href="https://en.wikipedia.org/wiki/Reinforcement_learning#/media/File:Reinforcement_learning_diagram.svg">Wikipedia</a></figcaption></figure> <p>When modeling this kind of scenario, a good mathematical framework is the Markov Decision Process. The Markov property, states that “future states depend only on the current state and action, not the whole history.” This is important for us, as when the agent takes an action within the environment from some state, it may obtain a reward along the way, but the next state it lands in is only dependent upon the <strong>current state</strong> and <strong>action taken</strong>. The medium article by <a href="https://medium.com/u/e72efd68f217">Nan</a> below does an amazing job of explaining Markov Decision Processes with a grid world example.</p> <p><a href="https://medium.com/@ngao7/markov-decision-process-basics-3da5144d3348">Markov decision process: basics</a></p> <p>Given that you have a perfect model of your environment, meaning your <strong>transition function</strong> and <strong>reward function</strong> are accurate enough, we can use <strong>exact solution methods</strong> to obtain the optimal value for each state. The basic two exact solution methods I started with are Value Iteration and Policy Iteration.</p> <p>Value iteration involves iteratively updating the utility values for each state using the Bellman Update until we converge on the optimal value function <em>V*</em> for each state.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*PrxAq5eLq7j1fnmr_-QErg.png"/><figcaption>Bellman Update | Source: <a href="https://algorithmsbook.com/decisionmaking/">Algorthms for Decision Making</a></figcaption></figure> <p>We can also use Policy Iteration, where we iteratively go through two steps, policy evaluation and policy improvement until we converge on the optimal policy for each state. Policy iteration can be more efficient than value iteration in certain cases and it requires fewer iterations to converge.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*XO1OR7WFp69Emlq-wtY1tQ.png"/><figcaption>Policy Iteration Process | Source: <a href="https://algorithmsbook.com/decisionmaking/">Algorthms for Decision Making</a></figcaption></figure> <p>Two other articles also written by <a href="https://medium.com/u/e72efd68f217">Nan</a> are great resources for obtaining a thorough understanding of Value Iteration and Policy Iteration.</p> <ul><li><a href="https://medium.com/@ngao7/markov-decision-process-value-iteration-2d161d50a6ff">Markov decision process: value iteration with code implementation</a></li><li><a href="https://medium.com/@ngao7/markov-decision-process-policy-iteration-42d35ee87c82">Markov decision process: policy iteration with code implementation</a></li></ul> <p><strong><em>But what happens when we don’t know the transition function or the reward function for the environment?</em></strong></p> <p>We can’t use exact solution methods anymore, but we can use <strong>Model-Free Methods</strong> to learn the value function or policy directly by interacting with the environment. One such method is called <strong>Q-Learning</strong> which learns the Q values (i.e. expected cumulative rewards for taking an action in a state) for each state-action pair. Q-learning achieves this by making a table to store the Q values for all states and all actions, then interacting with the environment for a specified number of episodes (a certain number of steps), and updating the Q value for each state-action pair.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*WsgV0V7p8ToJ4V5P6H0Yzw.png"/><figcaption>Q-learning Update Step | Knowledge Source: <a href="https://algorithmsbook.com/decisionmaking/">Algorithms for Decision Making</a></figcaption></figure> <p>For a deeper understanding of Q-Learning, I have linked an article below that is detailed and helps to visualize the process.</p> <p><a href="https://towardsdatascience.com/reinforcement-learning-explained-visually-part-4-q-learning-step-by-step-b65efb731d3e">Reinforcement Learning Explained Visually (Part 4): Q Learning, step-by-step</a></p> <p><strong><em>What if we cannot simply model the environment with a discrete state space?</em></strong></p> <p>If we require a continuous state space, we cannot get away with storing all the Q-values in a table anymore as we now have a potentially infinite number of states for our agent to interact with.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/0*i7iJQZozwXaqB_zE.jpg"/><figcaption>Q Learning vs Deep Q Learning | Source: <a href="https://huggingface.co/learn/deep-rl-course/en/unit3/from-q-to-dqn">HuggingFace</a></figcaption></figure> <p>Deepmind came up with a solution for this in 2013 with the paper “Playing Atari with Deep Reinforcement Learning.” They showed that by introducing neural networks to approximate the Q-value function, they could scale to large state spaces.</p> <p><a href="https://arxiv.org/abs/1312.5602">Playing Atari with Deep Reinforcement Learning</a></p> <p>For a deeper understanding of DQN, I highly recommend the articles linked below. They both cover visual explanations of concepts that make DQN perform well, like two Q-networks (a main network and a target network) and an experience replay buffer (we sample random batches from this) which is used to break the correlation between sequential experiences.</p> <ul><li><a href="https://huggingface.co/learn/deep-rl-course/en/unit3/deep-q-algorithm">The Deep Q-Learning Algorithm - Hugging Face Deep RL Course</a></li><li><a href="https://towardsdatascience.com/reinforcement-learning-explained-visually-part-5-deep-q-networks-step-by-step-5a5317197f4b">Reinforcement Learning Explained Visually (Part 5): Deep Q Networks, step-by-step</a></li></ul> <p><strong><em>What if we don’t know our current state in the environment?</em></strong></p> <p>The real world has noisy sensor readings and other uncertainties that make modeling environments using just MDPs difficult. We can then model the environment as a Partially Observable Markov Decision Process. A POMDP now has <em>O</em>, a set of possible observations, and <em>Z</em>, a function that maps to an observation given the next state obtained from an action. Since we do not know the state directly, we can make observations and infer the possible state of the agent by maintaining a belief state.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*r_7-dvY2hPP7B8SjnpfJwg.png"/><figcaption>Tuple for POMDP | Knowledge Source: <a href="https://algorithmsbook.com/decisionmaking/">Algorithms for Decision Making</a></figcaption></figure> <p>There already exist techniques to solve POMDPs like QMDP, SARSOP, MCVI, and more. To learn more details regarding POMDPs, please go to the site below.</p> <p><a href="https://www.pomdp.org/tutorial/pomdp-background.html">Background on POMDPs</a></p> <p><strong><em>Deep Recurrent Q Networks</em></strong></p> <p>For larger, more complex state spaces in partially observable environments, traditional POMDP solvers often become computationally impractical. Here, Deep Recurrent Q Networks (DRQNs) offer a deep learning-based solution by using recurrent neural networks (like LSTMs) to learn and retain a memory of past observations and actions, helping to approximate the belief state without explicit belief tracking. Long Short-Term Memory (LSTM) networks are used by DRQNs to implicitly learn the underlying state.</p> <p><a href="https://arxiv.org/abs/1507.06527">Deep Recurrent Q-Learning for Partially Observable MDPs</a></p> <p>The page below provides an implementation of a DRQN. Compared to a DQN, the first fully connected layer is replaced by an LSTM layer in DRQN.</p> <h3>Deep Transformer Q Networks</h3> <p>If using an RNN (and changing the model) can improve the performance of a DQN, couldn’t transformers improve the performance even further? That is basically what was explored by Esslinger et al. in their paper “<a href="https://arxiv.org/abs/2206.01078">Deep Transformer Q-Networks for Partially Observable Reinforcement Learning</a>.”</p> <p>Transformers are shown to be capable of modeling sequences better than RNNs and the <a href="https://arxiv.org/abs/1706.03762">attention</a> mechanism in transformers allows the model to weigh the importance of different parts of the input. Attention is especially helpful in situations like these where we require long-term planning.</p> <p>For an in-depth understanding of transformers, please refer to the article below, with a particular focus on the decoder portion of the text embedding section.</p> <p><a href="https://medium.com/correll-lab/building-clip-from-scratch-68f6e42d35f4">Building CLIP From Scratch</a></p> <p><strong><em>Architecture</em></strong></p> <p>The architecture of a DTQN consists of three main components: Observation Embedding, Transformer Decoder, and Q-Value Head. The input embedding layer encodes the sequence of observations and actions into a suitable format for the transformer. The transformer Decoder then processes these embeddings (leveraging self-attention to model dependencies) and generates an output. We then have a linear transformation to obtain Q-values for each possible action.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*aKZaNnBNS1kuAk4t3QmmBg.png"/><figcaption>DTQN Architecture | Source: <a href="https://arxiv.org/abs/2206.01078">Deep Transformer Q-Networks for Partially Observable Reinforcement Learning</a></figcaption></figure> <p>From the image of the architecture above, we can see that we have an input embedding, which takes the current and past observations. There is an <em>optional</em> positional encoding portion that can then be added to this. The embedding is then added to the decoder block, which is a stack of Self-Attention, Combine, LayerNorm, and FeedForward layers. The generated output of the decoder is then fed into the Q-value head (simple linear transformation) and we then obtain the Q-values for each possible action. We calculate loss similar to DQN, take action with the highest Q-value, interact with the environment, and finally add the new observation to the embedding to repeat the whole process over again.</p> <p><strong><em>Experiments</em></strong></p> <p>The authors compared the performance of DTQN against other networks such as DRQN, DQN, and a basic attention network. They compared performances across several domains such as <a href="https://github.com/abaisero/gym-gridverse">gym-gridverse</a>, and car flag.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/285/1*iL7uRTNKWQt7_Eh2VGXa6Q.png"/><figcaption>Gym Gridverse Memory 7x7 | Source: <a href="https://arxiv.org/abs/2206.01078">DTQN Paper</a> | Note: the bottom part is what the agent “sees”</figcaption></figure> <p>They ran ablation studies to potentially catch which parts of the architecture contributed the most. The base DTQN they used had standard residual connections, where a LayerNorm is applied after the residual connection step. They performed an ablation study by adding gating mechanisms similar to “Gated Recurrent Units” in the “Combine” step in the decoder. They also compared to an Identity map re-ordering variant of the decoder, where LayerNorm occurs before sublayers like “Self-Attention.”</p> <p>The authors also compared a learned positional encoding versus a sinusoidal positional encoding variant in the study. For the last major comparison, they choose to compare between using the Q-value at the last timestep or the intermediate Q-values at each timestep.</p> <p><strong><em>Running DTQN locally</em></strong></p> <p>Thankfully, the authors provide a modular set of code that can let us also run DTQN and observe comparisons. The results obtained by the authors can be seen in the <a href="https://arxiv.org/pdf/2206.01078">original paper</a> where it is well visualized.</p> <p>I ran some experiments locally with the code available at the repo linked below, and I will include those graphs here alongside instructions on getting set up.</p> <p><a href="https://github.com/kevslinger/DTQN/tree/paper">GitHub - kevslinger/DTQN at paper</a></p> <p>First of all, please make sure that you clone the “paper” branch of the repo to get the most stable version for replicating the original results.</p> <p>Given your operating system, some of the instructions on the repo may be outdated, so I’ve included a few extra steps below. If your system cannot make a virtual environment for Python 3.8, you can add older versions with the following command:</p> <pre>sudo add-apt-repository ppa:deadsnakes/ppa</pre> <p>Note, that I used Ubuntu 24.04 to run the experiments, and I did not have Python 3.8 available natively. You can then make a virtual environment following the commands from the repo.</p> <pre>virtualenv venv -p 3.8<br />source venv/bin/activate</pre> <p>At this point, you should check the version of pip. Quite a few of the requirements listed in the repo are old, and reference requirements with an older style, which can break many installations if you are using a newer version of pip. I downgraded to pip 21.1.3 in order for things to install smoothly.</p> <pre>pip install pip==21.1.3</pre> <p>You can then install all files from the requirements, as mentioned in the repo.</p> <pre>pip install -r requirements.txt</pre> <p>I also recommend installing gym-gridverse, rl-parsers, one-to-one, and gym-pomdps as mentioned in the repo (in that order.)</p> <p><strong><em>Results</em></strong></p> <p>We can save and visualize the policies obtained from training the agents. The repo suggested using the website <a href="http://wandb.ai">wandb.ai</a>, which I would also recommend as it makes it simple to create graphs and have the data in one place. I ran a DQN, DRQN, and DTQN version of the CarFlag environment, each one took ~2 hours to train locally on my GPU.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/595/0*-cdmVkhs1yUpwr77.png"/><figcaption>CarFlag Domain | Source: <a href="https://github.com/hai-h-nguyen/pomdp-domains">POMDP Domains</a></figcaption></figure> <p>In the CarFlag domain, the agent must go to the green flag (can be on any side). If it goes to the red flag, it is penalized (-1). If it goes to the blue flag, the agent learns the direction of the green flag (reward of +1).</p> <p>Initially, we said that the goal of an agent in reinforcement learning is to maximize its final reward. From the image below (learning curves), we can see that in the CarFlag environment, DTQN provided the best return as it was allowed to take steps in the environment and train.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*NwSgU0asB4NWH7vVZez7yg.png"/><figcaption>Mean Return Obtained | Source: <a href="https://github.com/kevslinger/DTQN/tree/paper">DTQN</a> and Own Work</figcaption></figure> <p>We also have data on how successful the agent was with every algorithm.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*Q-E02isnFXwOX7i1Q_m7rQ.png"/><figcaption>Mean Success Rate | Source: <a href="https://github.com/kevslinger/DTQN/tree/paper">DTQN</a> and Own Work</figcaption></figure> <p><strong><em>Discussion of Results</em></strong></p> <p>The paper shows results of DTQN outperforming (or comparable performance to) DRQN in most environments in which it was tested. The ablation results only show a significant improvement when Intermediate Q-values are trained on (i.e. train on the Q-values generated for each timestep in the observation history). The other ablations do not seem to offer a significant performance improvement. One interesting thing to note from the results obtained by the authors is that they can now visualize the most important parts of an agent&#39;s history of observations for decision-making. I find this quite fascinating for explainable AI in an RL context.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/700/1*uyXqoHgIqtDmDPB86dcPiw.png"/><figcaption>Attention bars | Source: <a href="https://arxiv.org/abs/2206.01078">Deep Transformer Q-Networks for Partially Observable Reinforcement Learning</a></figcaption></figure> <p>The authors provided a great example of the observation history in the Gridverse domain, with high attention weights highlighted in the image.</p> <p>I would like to note, however, that the environments being tested in the paper were rather small. Both state and action spaces were limited in the tested domain. A larger domain is necessary to see how well this method can compare to DRQN and traditional POMDP solvers.</p> <h3>Sources</h3> <ol><li><em>Deep Transformer Q-Networks for Partially Observable Reinforcement Learning</em>. arXiv preprint, 2022. Retrieved from <a href="https://arxiv.org/abs/2206.01078">arxiv.org/abs/2206.01078</a>.</li><li><em>Reinforcement Learning</em>, Wikipedia. Retrieved from <a href="https://www.wikiwand.com/en/articles/Reinforcement_learning">wikiwand.com</a>.</li><li><em>Algorithms for Decision Making</em>. Retrieved from <a href="https://algorithmsbook.com/decisionmaking/">algorithmsbook.com</a>.</li><li><em>From Q-learning to Deep Q-networks (DQN)</em>, Hugging Face Deep RL Course. Retrieved from <a href="https://huggingface.co/learn/deep-rl-course/en/unit3/from-q-to-dqn">huggingface.co</a>.</li><li><em>DTQN Implementation</em>, GitHub repository. Retrieved from <a href="https://github.com/kevslinger/DTQN/tree/paper">github.com/kevslinger</a>.</li></ol> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=e7efd9379e5f" width="1" height="1" alt=""/>&lt;hr&gt;&lt;p&gt;<a href="https://medium.com/correll-lab/deep-transformer-q-networks-a-paper-analysis-e7efd9379e5f">Deep Transformer Q Networks — A paper analysis</a> was originally published in <a href="https://medium.com/correll-lab">Toward Humanoids</a> on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry><entry><title type="html">CLIP implementation with pre-trained embeddings</title><link href="https://aritrac.com/blog/2024/clip-implementation-with-pre-trained-embeddings/" rel="alternate" type="text/html" title="CLIP implementation with pre-trained embeddings"/><published>2024-10-08T04:15:26+00:00</published><updated>2024-10-08T04:15:26+00:00</updated><id>https://aritrac.com/blog/2024/clip-implementation-with-pre-trained-embeddings</id><content type="html" xml:base="https://aritrac.com/blog/2024/clip-implementation-with-pre-trained-embeddings/"><![CDATA[<h4>Using ResNet50 and BERT to speed up training and increase accuracy</h4> <p>OpenAI published Contrastive Learning Image Pretraining, or <a href="https://openai.com/index/clip/">CLIP</a> for short on January 5, 2021, alongside <a href="https://openai.com/index/dall-e/">DALL-E</a>. Both algorithms were state-of-the-art multimodal algorithms as CLIP could describe text from images and DALL-E could create images from text.</p> <p>Our focus today will be on implementing a version of CLIP from scratch, with some explanations that can hopefully be helpful. A lot of the work in this article is derived from <a href="https://medium.com/u/b66aa802e02f">Matt Nguyen</a>’s work as I took inspiration from his <a href="https://medium.com/correll-lab/building-clip-from-scratch-68f6e42d35f4">Building CLIP From Scratch</a> article, including the code he used in a <a href="https://colab.research.google.com/drive/1E4sEg7RM8HBv4PkIhjWZuwCXXbB_MinS?usp=sharing">Google Colab Implementation</a>. It will be helpful for the purposes of this article if one has a background understanding of encoders, which is a part of the ViT implementation linked below.</p> <p><a href="https://medium.com/correll-lab/building-a-vision-transformer-model-from-scratch-a3054f707cc6">Building a Vision Transformer Model From Scratch</a></p> <p>We can begin by analyzing the algorithm, and then move on to two different implementations, one that simplifies things, and one that then builds on top of it.</p> <h3>The Algorithm</h3> <p><a href="https://arxiv.org/abs/2103.00020">Learning Transferable Visual Models From Natural Language Supervision</a></p> <p><em>The Pseudocode of the original CLIP implementation is available at the link above on page 5, the explanation for which is provided below.</em></p> <p>The following steps can be outlined to describe what is happening in CLIP at a high level:</p> <ol><li><em>Data Preparation</em> — The data usually needs to be pre-processed in a way that allows both the image encoder and text encoder to work with it (eg. resizing or normalizing images). The goal is to compare how similar both encodings will be, so we need to ensure there is some kind of text-based description or label for future usage. We also need to ensure we have enough data samples, as the best way for the algorithm to learn is through enough data points. The <a href="https://huggingface.co/datasets">database</a> library on HuggingFace is a great place to find a suitable dataset!</li><li><em>Image Encoder</em> — An encoder takes the high-dimensional information that is available in a source (an image in this case) and essentially “compresses” it down to a lower-dimensional or smaller representation of it. The “summary” is called an embedding. For images, we can achieve this through either a <a href="https://arxiv.org/abs/2010.11929">Vision Transformer</a> or a <a href="https://arxiv.org/abs/1512.03385">Convolutional Neural Network</a>.</li><li><em>Text Encoder</em> — Similar to above, we encode the information in a sentence to an embedding of it in this part. From my understanding, in order to capture context in sentences and keep meaning intact, <a href="https://arxiv.org/abs/1706.03762">Transformers</a> are the best method to use for now. There are also simpler methods like <a href="https://arxiv.org/abs/1301.3781">word2vec</a> which can essentially achieve similar results, albeit the quality will be significantly worse, as word2vec does not capture context.</li><li><em>Calculate Embedding Similarity &amp; Contrastive Loss </em>— Once both the image embedding and text embeddings are available and in the same embedding space, the goal of CLIP is to figure out how similar they are. We can use a technique like cosine similarity to measure how close the embeddings are. The image below gives a simplified visualization of this process. Then, we simply train the model to bring the correct matches closer together while pushing the wrong ones further out in a vector space. This section is just to provide a method for the training portion to follow.</li><li><em>Train</em> — We can train the model once both the image and text embeddings have been calculated. We provide a large number of image-text pairs to the model, calculate the similarity between the embeddings, and adjust the model to pull correct pairs closer together while pushing apart incorrect pairs.</li><li><em>Evaluate </em>— We need to see how well the model can match new images and text together. How well can the model describe new pictures? Zero-shot classification is also a cool part of the CLIP paper, where the model could obtain the correct descriptions for images on the first try! We can evaluate these to see how well it performs.</li></ol> <figure><img alt="" src="https://cdn-images-1.medium.com/max/510/1*bwtGNjnr89QcZ2ye8shJpA.png"/><figcaption>Fig 1: A simplified projection of embeddings in different dimensions being compared | Source: Own Work</figcaption></figure> <p>This diagram shows a simplified example in three dimensions, where the Image Embedding is captured in a 3-dimensional vector and the Text Embedding is captured in a 2D vector that lies on the xy plane. In order to get a cosine similarity between the two embeddings, we must first project the Image embedding on to the xy plane. We can then calculate the cosine similarity between the two vectors. In reality however, the dimensionality is much higher than this, so this is only an easy way to visualise the process. With CLIP, the goal is to determine how well an image matches a given text description, and this can be done by calculating the <a href="https://www.sciencedirect.com/topics/computer-science/cosine-similarity#:~:text=Cosine%20similarity%20measures%20the%20similarity,document%20similarity%20in%20text%20analysis.">cosine similarity</a> between their embeddings.</p> <h3>Experimentation with Pre-Trained Embeddings</h3> <p>Now that we have an understanding of how CLIP works, we can implement it. However, it has already been implemented from scratch before, so I wanted to experiment with things a bit. In the original <a href="https://colab.research.google.com/drive/1E4sEg7RM8HBv4PkIhjWZuwCXXbB_MinS?usp=sharing">Google Colab</a> notebook by <a href="https://medium.com/u/b66aa802e02f">Matt Nguyen</a>, he used a Vision Transformer built from scratch to obtain the image encodings, and a transformer (tokenizer too) that he built to get the text encodings. What if we replaced these transformers to use different pre-trained options? How well would this work for something like the FashionMNIST dataset if we use pre-trained encoders?</p> <p>The Google Colab notebook for this part can be found <a href="https://colab.research.google.com/drive/1mrkKfKxGPTO77iAvDDx9S9GbL36N5fx_?usp=sharing">here</a>.</p> <h4>Code</h4> <p>After importing the libraries,</p> <pre>import torch<br />import torch.nn as nn<br />import torch.optim as optim<br />import torchvision.transforms as T<br />from torch.utils.data import Dataset, DataLoader<br />from datasets import load_dataset<br />import matplotlib.pyplot as plt<br />import numpy as np</pre> <p>we can choose a text encoder. We can use <a href="https://arxiv.org/abs/1810.04805">BERT</a> as it is a famous model built with transformers, and the tokenizer model should be useful for our purposes. We have to make sure that we freeze the parameters for this model, as the goal is to find out how well it works off the shelf. The projection to the embedding dimension should, however, still be trainable.</p> <pre>from transformers import BertTokenizer, BertModel<br /><br />class TextEncoder(nn.Module):<br />    def __init__(self, emb_dim):<br />        super().__init__()<br /><br />        # Using a pre-trained BERT model<br />        self.bert = BertModel.from_pretrained(&#39;bert-base-uncased&#39;)<br />        for param in self.bert.parameters():<br />            param.requires_grad = False  # Freeze BERT parameters<br /><br />        # Adding a linear projection to get to the embedding dimension<br />        self.projection = nn.Linear(self.bert.config.hidden_size, emb_dim)<br /><br />    def forward(self, text):<br />        # Tokenizing text before passing it in, also not training this<br />        with torch.no_grad():<br />            outputs = self.bert(**text)<br />            x = outputs.last_hidden_state[:, 0, :]<br /><br />        # Projecting to wanted dimension<br />        x = self.projection(x) # Trainable Projection<br />        x = x / torch.norm(x, dim=-1, keepdim=True)  # L2 normalization<br /><br />        return x</pre> <p>Similarly, we can replace the Image Encoder with a CNN-based network. I chose ResNet50 for the purpose of this tutorial, as I expected it to run faster in comparison to <a href="https://huggingface.co/docs/transformers/en/model_doc/vit">ViT</a>. I was also curious as to how well a CNN embedding would perform for the overall model. We are also freezing parameters and projecting to an embedding dimension here. We have to remember to remove the final classification layer, as the goal is to simply pass the embedding information along. The final output layer has an output dimension of 2048, <a href="https://towardsdatascience.com/the-annotated-resnet-50-a6c536034758">before the classification layer</a>.</p> <pre>from torchvision import models<br />import torch.nn.functional as F<br /><br />class ImageEncoder(nn.Module):<br />    def __init__(self, emb_dim):<br />        super().__init__()<br /><br />        # Using a pre-trained ResNet for image embeddings<br />        self.resnet = models.resnet50(weights=&#39;IMAGENET1K_V1&#39;)<br />        for param in self.resnet.parameters():<br />            param.requires_grad = False  # Freeze ResNet parameters<br />        <br />        # Removing the final classification layer<br />        self.resnet = nn.Sequential(*list(self.resnet.children())[:-1]) <br />        <br />        # Final output layer dimension<br />        self.output_dim = 2048<br /><br />        # Adding a linear projection to get to the embedding dimension<br />        self.projection = nn.Linear(self.output_dim, emb_dim)<br /><br />    def forward(self, x):<br />        # Extracting image features using ResNet, but not training<br />        with torch.no_grad():<br />            x = self.resnet(x)  # Shape: (batch_size, 2048, 1, 1)<br />        x = x.view(x.size(0), -1) # Flatteing output to: (batch_size, 2048)<br /><br />        # Projecting to wanted dimension<br />        x = self.projection(x)  # Shape: (batch_size, emb_dim)<br />        x = F.normalize(x, p=2, dim=-1)  # L2 normalization<br /><br />        return x</pre> <p>We can then set up the CLIP algorithm where we get image and text embeddings, and then we can calculate the scaled pairwise cosine similarities between every image-text pair in the batch. After that, we calculate the loss function. The latter parts of the function including the cosine similarities calculation and loss function calculation are the same as the <a href="https://medium.com/correll-lab/building-clip-from-scratch-68f6e42d35f4">Building CLIP From Scratch</a> tutorial.</p> <pre>class CLIP(nn.Module):<br />    def __init__(self, emb_dim):<br />        super().__init__()<br /><br />        # Use the previously created encoders<br />        self.image_encoder = ImageEncoder(emb_dim)<br />        self.text_encoder = TextEncoder(emb_dim)<br /><br />        # Temperature parameter for scaling cosine similarity<br />        self.temperature = nn.Parameter(torch.ones([]) * np.log(1 / 0.07))<br />        self.device = torch.device(&quot;cuda&quot; if torch.cuda.is_available() else &quot;cpu&quot;)<br /><br />    def forward(self, image, text):<br />        I_e = self.image_encoder(image)<br />        T_e = self.text_encoder(text)<br /><br />        # cosine similarities<br />        logits = (I_e @ T_e.transpose(-2, -1)) * torch.exp(self.temperature)<br /><br />        # loss function<br />        labels = torch.arange(logits.shape[0]).to(self.device)<br />        loss_i = nn.functional.cross_entropy(logits.transpose(-2, -1), labels)<br />        loss_t = nn.functional.cross_entropy(logits, labels)<br />        loss = (loss_i + loss_t) / 2<br /><br />        return loss</pre> <p>For the next portion, we need to create a class for the Dataset. We need to pre-process the dataset so that we can return valid information that is useful to the ResNet50 and BERT models. FashionMNIST doesn’t come with generated captions, instead it comes with labels for each class. We simply have to add some captions to each class. We also make each image “RGB” by simply repeating the image 3 times, as that is the format ResNet50 expects.</p> <pre>class FashionMNIST(Dataset):<br />    def __init__(self, train=True):<br />        self.dataset = load_dataset(&quot;fashion_mnist&quot;)<br />        self.transform = T.ToTensor() # need this for ResNet<br />        self.tokenizer = BertTokenizer.from_pretrained(&#39;bert-base-uncased&#39;)<br /><br />        # This is for future splitting of the dataset<br />        self.split = &quot;train&quot; if train else &quot;test&quot;<br /><br />        # Creating captions to map the labels to<br />        self.captions = {<br />            0: &quot;An image of a t-shirt/top&quot;,<br />            1: &quot;An image of trousers&quot;,<br />            2: &quot;An image of a pullover&quot;,<br />            3: &quot;An image of a dress&quot;,<br />            4: &quot;An image of a coat&quot;,<br />            5: &quot;An image of a sandal&quot;,<br />            6: &quot;An image of a shirt&quot;,<br />            7: &quot;An image of a sneaker&quot;,<br />            8: &quot;An image of a bag&quot;,<br />            9: &quot;An image of an ankle boot&quot;<br />        }<br /><br />    def __len__(self):<br />        return self.dataset.num_rows[self.split]<br /><br />    def __getitem__(self, i):<br />        img = self.dataset[self.split][i][&quot;image&quot;]<br />        img = self.transform(img)<br /><br />        # Converting 1-channel grayscale image to 3-channel RGB<br />        img = img.repeat(3, 1, 1)<br /><br />        # matching caption to label<br />        label = self.dataset[self.split][i][&quot;label&quot;]<br />        caption = self.captions[label]<br /><br />        # Tokenizing the caption<br />        # The tokenizer usually returns an extra dimension<br />        #    so we remove that with squeeze(0)<br />        encoded_caption = self.tokenizer(caption, return_tensors=&#39;pt&#39;, padding=&#39;max_length&#39;, truncation=True, max_length=32)<br />        input_ids = encoded_caption[&#39;input_ids&#39;].squeeze(0)<br />        attention_mask = encoded_caption[&#39;attention_mask&#39;].squeeze(0)<br /><br />        # returning everything as flat tensors, this could effect performance<br />        return img, input_ids, attention_mask, label</pre> <p>We can then set up the hyperparameters and load the dataset for usage in training.</p> <pre>emb_dim = 512  <br />lr = 1e-4     <br />epochs = 10<br />batch_size = 64<br /><br />train_set = FashionMNIST(train = True)<br />test_set = FashionMNIST(train = False)<br /><br />train_loader = DataLoader(train_set, shuffle=True, batch_size=batch_size)<br />test_loader = DataLoader(test_set, shuffle=False, batch_size=batch_size)</pre> <p>The training loop is in the code block below. We initialize the CLIP model with the embedding dimension that want and set up the optimizer. Since we want to see how the model performs using off-the-shelf embeddings, we have to set up which parameters will be optimized. We only want to optimize the embedding projections and the temperature. The loop then iterates over epochs, where the model computes the loss by comparing the cosine similarities between the text and image embeddings. We then perform backpropagation where the gradients are calculated and the model’s parameters are updated to minimize the loss. The best model is saved at the end of each epoch. I also added a timer in order to get a sense of training time.</p> <pre>device = torch.device(&quot;cuda&quot; if torch.cuda.is_available() else &quot;cpu&quot;)<br />print(&quot;Using device: &quot;, device, f&quot;({torch.cuda.get_device_name(device)})&quot; if torch.cuda.is_available() else &quot;&quot;)<br /><br /># Initializing CLIP<br />model = CLIP(emb_dim).to(device)<br /><br /># Optimizer setup<br /># Collect parameters to optimize<br />params = list(model.image_encoder.projection.parameters()) + \<br />         list(model.text_encoder.projection.parameters()) + \<br />         [model.temperature]<br /><br />optimizer = optim.Adam(params, lr=lr)<br /><br />best_loss = np.inf<br />for epoch in range(epochs):<br />    start_time = time.time()<br />    model.train()  # Set model to training mode<br /><br />    for batch in train_loader:<br />        # unpacking the whole batch for training<br />        images = batch[0].to(device)<br />        input_ids = batch[1].to(device)<br />        attention_mask = batch[2].to(device)<br /><br />        text_inputs = {&quot;input_ids&quot;: input_ids, &quot;attention_mask&quot;: attention_mask}<br />        loss = model(images, text_inputs)<br /><br />        # Backpropagation and optimization<br />        optimizer.zero_grad()<br />        loss.backward()<br />        optimizer.step()<br /><br />    # Logging the loss<br />    print(f&quot;Epoch [{epoch + 1}/{epochs}], Loss: {loss.item():.4f}&quot;)<br /><br />    # Save the best model<br />    if loss.item() &lt; best_loss:<br />        best_loss = loss.item()<br />        torch.save(model.state_dict(), &quot;clip_model.pt&quot;)<br />        print(&quot;Model Saved.&quot;)<br /><br />    end_time = time.time()<br />    print(f&quot;Time taken for epoch {epoch + 1}: {end_time - start_time:.2f} seconds&quot;)</pre> <p>Next, we set up the evaluation loop, starting by loading the trained model and setting it to evaluation mode. We begin by precomputing the text embeddings for the class descriptions, as these embeddings remain fixed throughout the testing process.</p> <p>For each batch in the test dataset, we extract the images and their corresponding true labels. We then generate image embeddings using the image encoder and calculate the cosine similarity between these embeddings and the pre-computed text embeddings. This similarity score quantifies how well the image aligns with each class description. The predicted label is determined by identifying the class with the highest similarity score.</p> <p>Finally, we compare the predicted labels to the true labels, tallying the correct predictions to compute the overall test accuracy.</p> <pre># Loading saved model and setting up for evaluation<br />model = CLIP(emb_dim).to(device)<br />model.load_state_dict(torch.load(&quot;clip_model.pt&quot;))<br />model.eval()<br /><br /># computing the embeddings for the class labels<br />class_names = [&quot;An image of a t-shirt or top&quot;, &quot;An image of trousers&quot;, &quot;An image of a pullover&quot;, &quot;An image of a dress&quot;, &quot;An image of a coat&quot;,<br />               &quot;An image of sandals&quot;, &quot;An image of a shirt&quot;, &quot;An image of sneakers&quot;, &quot;An image of a bag&quot;, &quot;An image of ankle boots&quot;]<br /><br /># tokenizing the above labels<br />text_inputs = test_set.tokenizer(class_names, padding=True, truncation=True, return_tensors=&quot;pt&quot;).to(device)<br /># no_grad() here maybe unnecessary, the goal is to not train anything when pre-computing embeddings<br />with torch.no_grad():<br />    class_text_features = model.text_encoder(text_inputs)<br /><br />correct, total = 0, 0<br /><br />for batch in test_loader:<br />    images = batch[0].to(device)<br />    labels = batch[3].to(device)<br /><br />    with torch.no_grad():<br />        image_features = model.image_encoder(images)<br /><br />    # calculate similarities<br />    similarities = image_features @ class_text_features.T  <br /><br />    # predicted labels are the indices of the maximum similarity scores<br />    preds = similarities.argmax(dim=1)<br /><br />    # update accuracy<br />    correct += (preds == labels).sum().item()<br />    total += labels.size(0)<br /><br />print(f&quot;Test Accuracy: {correct / total:.4f}&quot;)</pre> <p>For the last step, we demonstrate the zero-shot classification capabilities of CLIP. Similar to the above, we load the saved model, pre-compute the text embeddings, and generate the image embeddings for a single image that we have not trained on. We calculate the cosine similarity, which allows the model to rank the classes, and finally, we display the top 5 predicted class names, along with their associated probabilities.</p> <pre># loading saved model<br />model = CLIP(emb_dim).to(device)<br />model.load_state_dict(torch.load(&quot;clip_model.pt&quot;))<br />model.eval()<br /><br />class_names = [&quot;An image of a t-shirt or top&quot;, &quot;An image of trousers&quot;, &quot;An image of a pullover&quot;, &quot;An image of a dress&quot;, &quot;An image of a coat&quot;,<br />               &quot;An image of sandals&quot;, &quot;An image of a shirt&quot;, &quot;An image of sneakers&quot;, &quot;An image of a bag&quot;, &quot;An image of ankle boots&quot;]<br /><br /># pre-computing text embeddings<br />text_inputs = test_set.tokenizer(class_names, padding=True, truncation=True, return_tensors=&quot;pt&quot;).to(device)<br />with torch.no_grad():<br />    text_features = model.text_encoder(text_inputs)<br /><br /># get image embeddings for image and then plot image<br />idx = 2000<br />img, _, _, label = test_set[idx]  # Unpack image and label<br />img_cpu = img.cpu()<br />plt.imshow(img_cpu.permute(1, 2, 0).squeeze(), cmap=&quot;gray&quot;)<br />plt.title(class_names[label])<br />plt.show()<br /><br />img_tensor = img.unsqueeze(0).to(device)<br />with torch.no_grad():<br />    # using model to calculate similarity<br />    image_features = model.image_encoder(img_tensor)<br />    similarity = (image_features @ text_features.T).softmax(dim=-1)<br />    values, indices = similarity[0].topk(5)<br /><br /># printing result<br />print(&quot;\nTop predictions:\n&quot;)<br />for value, index in zip(values, indices):<br />    print(f&quot;{class_names[int(index)]:&gt;30s}: {100 * value.item():.2f}%&quot;)</pre> <h4>Results Discussion</h4> <p>The question I attempted to answer in the above section is, ‘Can pre-trained models immediately replace the encoders in a CLIP implementation?’</p> <p>The test accuracy I obtained was 75.65%, whereas the test accuracy for the implementation from scratch was 84% (hyper-parameter tuning can certainly improve performance even further.) Considering that we do not perform any further training for ResNet or BERT on this dataset, I am quite impressed with the performance.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/874/1*BUg6X66eOlEjVJj3D0DPIA.png"/></figure> <figure><img alt="" src="https://cdn-images-1.medium.com/max/882/1*HoQ9Yzi57IubBkQTNuHhCQ.png"/><figcaption>Figure 2: Left - Predictions Made in <a href="https://colab.research.google.com/drive/1E4sEg7RM8HBv4PkIhjWZuwCXXbB_MinS?usp=sharing">CLIP From Scratch</a>, Right - Predictions made with <a href="https://colab.research.google.com/drive/1mrkKfKxGPTO77iAvDDx9S9GbL36N5fx_?usp=sharing">Pre-Trained Embedding</a></figcaption></figure> <p>One of my major points of concern, however, is that while the model is still correct around 76% of the time, the spread of probability over all the classes is a weakness. The original model was consistently more confident in the correct answer. This can be attributed to the fact that we did not fine-tune ResNet and BERT. It could also be because of the alignment differences in between ResNet and BERT. They both generate embeddings that work within their own contexts, but when they are used for dimensions specific to FashionMNIST, there might be less coherence in between the two.</p> <h3>Applying to a different Dataset</h3> <p>While setting up the functions in the previous notebook to have trainable layers for BERT and ResNet, I was curious about working with a different dataset. The <a href="https://huggingface.co/datasets">datasets</a> library is a great resource for finding something suitable. Since both BERT and ResNet were trained on diverse datasets, I wanted to check the capabilities of this local CLIP model if we also got a diverse dataset.</p> <p>While I initially started with a <a href="https://huggingface.co/datasets/yerevann/coco-karpathy">fork</a> of the COCO dataset containing image URLs and 5 sentences describing each image, the training time was too high because each image had to be downloaded. I decided to then move on to the Flickr8k dataset linked below, as the images came included and I only wanted to experiment with one caption per image (even though it has two more synthetically created.)</p> <p><a href="https://huggingface.co/datasets/kargwalaryan/SynCap-Flickr8k/viewer?row=0">kargwalaryan/SynCap-Flickr8k · Datasets at Hugging Face</a></p> <p>You can follow along with my implementation <a href="https://colab.research.google.com/drive/1UotqRT3jqpAN10P4ujctlE6m4KOgzNA5?usp=sharing">here</a>.</p> <h4>Code</h4> <p>We should not have to change too much in order to adapt the code to a new dataset.</p> <p>The library imports should remain the same, but we do need to change a few lines for the text encoder portion. We now open up the last three layers of BERT for training and the torch.nograd() portion is removed.</p> <pre>from transformers import BertTokenizer, BertModel<br /><br />class TextEncoder(nn.Module):<br />    def __init__(self, emb_dim):<br />        super().__init__()<br /><br />        self.bert = BertModel.from_pretrained(&#39;bert-base-uncased&#39;)<br />        <br />        # the last three layers of the network are open for training<br />        for name, param in self.bert.named_parameters():<br />            if &#39;encoder.layer.11&#39; in name or &#39;encoder.layer.10&#39; in name or &#39;pooler&#39; in name:<br />                param.requires_grad = True<br />            else:<br />                param.requires_grad = False<br /><br />        self.projection = nn.Linear(self.bert.config.hidden_size, emb_dim)<br /><br />    def forward(self, text):<br />        outputs = self.bert(**text)<br />        x = outputs.last_hidden_state[:, 0, :]<br /><br />        x = self.projection(x)<br />        x = x / torch.norm(x, dim=-1, keepdim=True)<br /><br />        return x</pre> <p>Similarly, we open up the last layer of ResNet for training in the ‘ImageEncoder’ block.</p> <pre>for name, param in self.resnet.named_parameters():<br />            if &#39;layer4&#39; in name:<br />                param.requires_grad = True<br />            else:<br />                param.requires_grad = False</pre> <p>The major change comes in the form of the dataset class. We can load the dataset but since all of it falls under the “train” split, we need to manually create a split ourselves. We also need to resize the images to 224 by 224 pixels and normalize them (based on ImageNet) as that is what is expected by ResNet in our Image Encoder class. We then extracted the caption and tokenized it using the BertTokenizer similar to the previous notebook.</p> <pre>class Flickr8kDataset(Dataset):<br />    def __init__(self, train=True):<br />        # loading dataset and splitting it<br />        dataset = load_dataset(&quot;kargwalaryan/SynCap-Flickr8k&quot;)<br />        split_dataset = dataset[&quot;train&quot;].train_test_split(test_size=0.2, seed=42)<br />        self.tokenizer = BertTokenizer.from_pretrained(&#39;bert-base-uncased&#39;)<br />        # resize the images, make it a tensor, then normalise it<br />        self.transform = T.Compose([<br />            T.Resize((224, 224)),<br />            T.ToTensor(),<br />            T.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])<br />        ])<br />        # Simple way to return split<br />        if train:<br />            self.dataset = split_dataset[&#39;train&#39;]<br />        else:<br />            self.dataset = split_dataset[&#39;test&#39;]<br /><br />    def __len__(self):<br />        return len(self.dataset)<br /><br />    def __getitem__(self, idx):<br />        # get the image and then convert it to remove any grayscale images<br />        # This is a more elegant method than repeating it thrice<br />        img = self.dataset[idx][&#39;image&#39;]<br />        img = img.convert(&quot;RGB&quot;)<br />        img = self.transform(img)<br />        # get the caption then tokenize it<br />        caption = self.dataset[idx][&#39;caption&#39;]<br />        encoded_caption = self.tokenizer(caption, return_tensors=&#39;pt&#39;, padding=&#39;max_length&#39;,<br />                                         truncation=True, max_length=32)<br />        # Getting the numerical ID&#39;s of the tokens and the attention mask<br />        input_ids = encoded_caption[&#39;input_ids&#39;].squeeze(0)<br />        attention_mask = encoded_caption[&#39;attention_mask&#39;].squeeze(0)<br />        return img, input_ids, attention_mask</pre> <p>We set up the training parameters and load the dataset like before, the main change occurs in the training loop. We now have to add some options to the optimizer that we used, as we have more parts that we want to change. For this experiment, I left the learning rate for ResNet and BERT lower than hyperparameter ‘lr’, and this can be tuned later to improve performance.</p> <pre>optimizer = optim.Adam([<br />    {&#39;params&#39;: model.image_encoder.resnet.parameters(), &#39;lr&#39;: 1e-5},<br />    {&#39;params&#39;: model.text_encoder.bert.parameters(), &#39;lr&#39;: 1e-5},<br />    {&#39;params&#39;: model.image_encoder.projection.parameters(), &#39;lr&#39;: lr},<br />    {&#39;params&#39;: model.text_encoder.projection.parameters(), &#39;lr&#39;: lr},<br />    {&#39;params&#39;: [model.temperature], &#39;lr&#39;: lr}<br />])</pre> <p>The testing loop will also be quite different as I focused more on checking the Image to Text retrieval capabilities of the model. We first load the model, then we load all the image and text embeddings. We then connect all embeddings which allows us to create a similarity matrix for all the pairs.</p> <p>For each image, we extract the corresponding row of the similarity matrix, as this row contains the similarity scores between the image and all the text embeddings. We then sort the similarities in descending order (highest similarity first), and then check for the index of the correct text embedding. We then calculate the rank, which is essentially where in the list the correct caption lies. The higher the rank, the worse it is.</p> <p>Finally, for some metrics, we calculate recall, which is the percentage of times the correct caption appears within the top N-ranked captions for each image.</p> <pre># Loading the model and enabling evaluation<br />model = CLIP(emb_dim).to(device)<br />model.load_state_dict(torch.load(&quot;clip_model.pt&quot;))<br />model.eval()<br /><br /># calculating the image and text embeddings for all items in test dataset<br />image_embeddings = []<br />text_embeddings = []<br /><br />with torch.no_grad():<br />    for batch in val_loader:<br />        images = batch[0].to(device)<br />        input_ids = batch[1].to(device)<br />        attention_mask = batch[2].to(device)<br />        text_inputs = {&quot;input_ids&quot;: input_ids, &quot;attention_mask&quot;: attention_mask}<br /><br />        image_features = model.image_encoder(images)<br />        text_features = model.text_encoder(text_inputs)<br /><br />        image_embeddings.append(image_features)<br />        text_embeddings.append(text_features)<br /><br /># connecting all embeddings<br />image_embeddings = torch.cat(image_embeddings, dim=0)<br />text_embeddings = torch.cat(text_embeddings, dim=0)<br /><br /># get the similarity for all image-text pairs<br />similarity_matrix = image_embeddings @ text_embeddings.T<br /><br /># see how well the model performed in relation to all options<br />ranks = []<br />for i in range(len(image_embeddings)):<br />    sims = similarity_matrix[i]<br />    sorted_indices = torch.argsort(sims, descending=True)<br />    rank = (sorted_indices == i).nonzero(as_tuple=True)[0].item() + 1<br />    ranks.append(rank)<br /><br /># calculate and display the recall, explained above<br />ranks = np.array(ranks)<br />recall_at_1 = np.mean(ranks &lt;= 1) * 100<br />recall_at_5 = np.mean(ranks &lt;= 5) * 100<br />recall_at_10 = np.mean(ranks &lt;= 10) * 100<br /><br />print(f&quot;Image-to-Text Retrieval:&quot;)<br />print(f&quot;Recall@1: {recall_at_1:.2f}%&quot;)<br />print(f&quot;Recall@5: {recall_at_5:.2f}%&quot;)<br />print(f&quot;Recall@10: {recall_at_10:.2f}%&quot;)</pre> <p>Instead of doing a zero-shot classification as we are not training on chunks of data, I wanted to visualize the final results and captions that are chosen. We first choose (randomly) the indices for the images we want to work with. Then we get the image embedding for them and calculate the similarity against the text embeddings. We then rank them and the top N captions are displayed alongside the picture.</p> <pre>import matplotlib.pyplot as plt<br /><br /># how many examples and captions do I want?<br />num_examples = 5<br />K = 5<br /><br /># randomly select indices from the validation dataset<br />indices = np.random.choice(len(val_dataset), num_examples, replace=False)<br /><br /># For each image<br />for idx in indices:<br />    img, input_ids, attention_mask = val_dataset[idx.item()]<br />    img_tensor = img.to(device).unsqueeze(0)<br />    <br />    # Get the image embedding and calculate similarity<br />    with torch.no_grad():<br />        image_feature = model.image_encoder(img_tensor)<br />        sims = image_feature @ text_embeddings.T<br />        sims = sims.squeeze(0)<br />    <br />    # get the top k captions for said image<br />    topk_indices = torch.argsort(sims, descending=True)[:K]<br />    topk_indices = [int(i.item()) for i in topk_indices]<br /><br />    topk_captions = [val_dataset.dataset[i][&#39;caption&#39;] for i in topk_indices]<br />    <br />    # Showing the top k images and their captions<br />    plt.imshow(img.permute(1, 2, 0).cpu())<br />    plt.axis(&#39;off&#39;)<br />    plt.show()<br /><br />    print(f&quot;Top {K} Captions for Image Index {idx}:&quot;)<br />    for i, caption in enumerate(topk_captions):<br />        print(f&quot;{i+1}: {caption}&quot;)<br />    print(&quot;\n&quot;)</pre> <h4>Results Discussion</h4> <p>The model did not perform as well as I had expected after FashionMNIST implementation.</p> <ul><li>The correct caption was in the first position — 12.12% of the time.</li><li>The correct caption was in the top 5 positions— 34.27% of the time.</li><li>The correct caption was in the top 10 positions — 47.52% of the time.</li></ul> <p>While I was immensely disappointed as I expected it to perform better, once I started looking at the possible captions for the images themselves it started to make sense to me.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*_nYj7jlaOVH-z078Fgg5hg.png"/></figure> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*rX1xASdTFRuMFJ3qCZMFug.png"/><figcaption>Figure 3: Examples of correct caption in position 1</figcaption></figure> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*Re44cYgK0-YfmSwCwgg2ig.png"/></figure> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*PUJ-h3KbnqpI46-Zy2s4SQ.png"/><figcaption>Figure 4: Examples of multiple possible captions</figcaption></figure> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*Q2NdtIvbnj_UHvJHwZENBw.png"/></figure> <figure><img alt="" src="https://cdn-images-1.medium.com/max/912/1*EVstwQR-oUvxjqGpbLoR2w.png"/><figcaption>Figure 5: Examples of multiple caption options</figcaption></figure> <p>The loss on the training data in the 15 epochs I trained had climbed down to 0.3921 from a starting point of 1.6041, so I expected better results. However, once we see the captions available, I see that there are many similar images with similar captions. So, even though the model is not getting the correct caption immediately, it can definitely focus on the important parts of the image and get near the ballpark of the correct text embedding.</p> <p>When the caption is correct in the first position, it’s because the image and captions were unique enough to be related. For example, in Figure 3b, the model correctly identifies the image with “A boy in a green shirt is pushing a shopping cart in which a boy in a yellow shirt is riding.” The scenario is so unique that the rest of the options (in the top 5) are not adequate to describe this.</p> <p>However, when we have many caption options for one image, the model struggles the most. For example, in Figure 5a, there is clearly a basketball player making a slam dunk, but the model is struggling to choose the correct option. The words “a basketball player” are clearly present in all the top 5 options, however, the embedding isn’t differentiated enough to describe it.</p> <p>I believe that using a ViT for the image encoder may fix some of these issues as the overall context could then be considered. Also, further training (fine tuning) can help lower the loss, but there is a fear of overfitting this new dataset.</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=9b2eca91dc22" width="1" height="1" alt=""/>&lt;hr&gt;&lt;p&gt;<a href="https://medium.com/correll-lab/clip-implementation-with-pre-trained-embeddings-9b2eca91dc22">CLIP implementation with pre-trained embeddings</a> was originally published in <a href="https://medium.com/correll-lab">Toward Humanoids</a> on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry></feed>